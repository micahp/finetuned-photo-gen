# Baseline Migration: credit rollover system

This migration was already applied directly in the production database before Prisma Migrate was introduced here.

The directory is intentionally empty so that Prisma's migration history matches the database state. 