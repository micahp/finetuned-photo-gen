// Placeholder — implementation under development
// Once `getModelsForProvider` is implemented this test suite will be re-enabled.

describe.skip('getModelsForProvider (pending implementation)', () => {
  it('placeholder', () => {
    expect(true).toBe(true)
  })
}) 